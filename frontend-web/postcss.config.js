/**
 * PostCSS Configuration
 * 
 * Configures PostCSS plugins for CSS processing.
 * Includes Tailwind CSS and Autoprefixer plugins for CSS transformation.
 * Used by the build process to process CSS files.
 */
module.exports = {
	plugins: {
		tailwindcss: {},
		autoprefixer: {},
	},
};
